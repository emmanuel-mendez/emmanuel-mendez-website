import { ResponsiveIncludes } from './includes';

describe('ResponsiveIncludes', () => {
  it('create an instance', () => {
    const pipe = new ResponsiveIncludes();
    expect(pipe).toBeTruthy();
  });
});
